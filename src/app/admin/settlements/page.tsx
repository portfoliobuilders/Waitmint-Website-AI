export default function AdminSettlementsPage() {
  return (
    <div>
      <h1 className="font-display text-4xl">Settlements</h1>
      <p className="mt-3 max-w-2xl text-sm text-[var(--wm-muted)]">
        Read-only by default. Impression, campaign, advertiser, user, gross, 60/40 split, timestamp, and
        duplicate state live on the Exchange. This website will not reconstruct money from the client.
      </p>
    </div>
  );
}
