export default function AdvertiserSettingsPage() {
  return (
    <div>
      <h1 className="font-display text-4xl">Advertiser settings</h1>
      <p className="mt-3 max-w-xl text-sm text-[var(--wm-muted)]">
        Organization membership is stored on the Exchange. Use the same WaitMint login to switch back to Earn.
      </p>
    </div>
  );
}
